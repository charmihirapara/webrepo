<!DOCTYPE html>
<html>
<head>
	<meta charset="utf-8">
	<meta name="viewport" content="width=device-width, initial-scale=1">
	<title>{% block title %}Block - Extends{% endblock %}</title>
</head>
<body>
	{% macro input(name, type, value) %}
		<input type="{{ type }}" name="{{ name }}" value="30">	
		{% deprecated 'The "welcome" macro is deprecated, use "hello" instead.' %}
	{% endmacro %}

	{# <p>{{ _self.input('username') }}</p> #}
</body>
</html